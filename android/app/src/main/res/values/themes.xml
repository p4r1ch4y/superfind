<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all real theming; this exists only so the window has a
         sensible background and no action bar before the first frame. -->
    <style name="Theme.Superfind" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@color/window_background</item>
        <item name="android:navigationBarColor">@color/window_background</item>
    </style>
</resources>
